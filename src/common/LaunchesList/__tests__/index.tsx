import { render } from "@testing-library/react";
import { LaunchesList } from "..";

describe("LaunchesList", () => {
  it("renders properly", () => {
    render(<LaunchesList>test</LaunchesList>);
  });
});